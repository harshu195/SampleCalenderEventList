<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".CalenderActivity"
   >

    <TextView android:text="Intent Calender" android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/tv1"
        android:textSize="35dp"
        android:textAlignment="center" />

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Show Calender"
        android:id="@+id/btnCal"
        android:layout_marginTop="84dp"
        android:layout_below="@+id/tv1"
        android:layout_alignRight="@+id/tv1"
        android:layout_alignEnd="@+id/tv1" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Add Event "
        android:id="@+id/btnAdd"
        android:layout_below="@+id/btnCal"
        android:layout_alignLeft="@+id/btnCal"
        android:layout_alignStart="@+id/btnCal"
        android:layout_marginTop="66dp" />

</RelativeLayout>
